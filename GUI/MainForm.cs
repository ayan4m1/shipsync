using Eto.Drawing;
using Eto.Forms;

namespace ShipSync.GUI
{
    internal class MainForm : Form
    {
        public MainForm()
        {
            Title = "ShipSync";
            ClientSize = new Size(800, 600);
        }   
    }
}